import styled from "styled-components";

const Button = styled.button`
  border: none;
  background: var(--info);
  padding: 12px;
  color: var(--white);
  border-radius: 4px;
  font-size: 14px;
  width: 100%;
  cursor: pointer;
`;

export default Button;
